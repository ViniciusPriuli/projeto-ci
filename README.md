# Projeto com CI/CD

Este projeto utiliza GitHub Actions para:

- Executar testes automatizados com pytest
- Empacotar o projeto
- Rodar pipeline a cada push ou pull request

## Como rodar localmente

pip install -r requirements.txt
pytest
